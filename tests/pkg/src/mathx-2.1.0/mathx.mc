// mathx.mc -- mathx 2.1.0: the release 2.1.0-rc1 was a candidate for, and the
// version every automatic choice inside major 2 has to reach.
i64 mathx_sq(i64 x) { return x * x; }
i64 mathx_version() { return 210; }
