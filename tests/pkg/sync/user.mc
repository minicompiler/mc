// user.mc -- the taught compiler tests/pkg/sync/teach.toml builds. It teaches
// nothing: what the fixture is for is the SHAPE -- a project with a [compiler]
// section builds a compiler and spawns it with --entry-only, and `mc build
// --sync` must sync exactly once, in the parent (M52 D7).
void user_init() {
}
