// quiet.mc — a module that teaches the compiler nothing. It exists so that
// tests/proj/teach-link.toml has a [compiler] to build: what is under test
// there is the ROAD the taught compiler is built by, not anything it teaches.
//
// Used by scripts/check-build.sh.
void user_init() {
}
