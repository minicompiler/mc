// mc_dollar.mc -- the compiler that carries lib/user_dollar.mc, the same
// three-line shape as lib/mc_coreop.mc. NOT in tools/bundle.list: it is a
// check-script-only demo, like user_badmach/user_coreop/user_remach.
#include "../src/host_macos.mc"
#include "../src/core.mc"
#include "user_dollar.mc"
