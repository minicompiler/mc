Measured against C, Go, Zig, Rust and C# on the same machine: **4.1 ms** to build this
program's own benchmark (`--exe`, no linker), a **33 KB** executable, and a **1.2 MB**
single-file toolchain with no separate linker or SDK to install. See
[how mc compares](comparison.md), with every command and every number.
